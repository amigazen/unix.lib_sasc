/* This is an example of a mountlist entry for APipe-Handler.  Please
   modify the `Handler' line according to where you put the APipe-Handler
   file.  Note that the file name should be specified with an absolute
   path!  */

APIPE:
    Handler = SDK:L/APipe-Handler
    Stacksize = 3000
    Priority = 5
    GlobVec = -1
#
